<section
  id="inicio"
  class="relative flex min-h-[calc(100dvh-2.5rem)] items-center overflow-hidden pt-20 pb-2 sm:pb-12"
>
  <div
    class="shell grid items-center gap-8 lg:grid-cols-[0.92fr_1.08fr] lg:gap-16"
  >
    <div class="hero-copy relative z-10">
      <p class="font-display mb-5 text-2xl font-bold sm:mb-7 sm:text-3xl">
        be<span class="text-signal">/</span>code
      </p>
      <h1 class="display-title">Software que hace avanzar.</h1>
      <p
        class="text-muted mt-5 max-w-lg text-base leading-7 sm:mt-7 sm:text-lg"
      >
        Productos web y sistemas de gestión pensados, diseñados y programados a
        medida.
      </p>
      <div class="mt-7 flex flex-wrap gap-3 sm:mt-9">
        <a
          href="#contacto"
          class="bg-signal text-surface hover:bg-signal-dark rounded-[var(--radius-ui)] px-6 py-3.5 font-bold transition-colors active:translate-y-px"
        >
          Hablemos <span aria-hidden="true">↗</span>
        </a>
        <a
          href="#proyectos"
          class="border-ink hover:bg-ink hover:text-surface rounded-[var(--radius-ui)] border px-6 py-3.5 font-bold transition-colors active:translate-y-px"
        >
          Ver proyectos
        </a>
      </div>
    </div>

    <div
      class="compiler"
      role="img"
      aria-label="Una especificación de producto convirtiéndose en una interfaz"
    >
      <div class="product-window">
        <div class="window-bar">
          <div class="brand-mark">b<span>/</span></div>
          <span>product.build / v0.8</span>
          <span class="live-state">Compilando</span>
        </div>
        <div class="window-body">
          <aside>
            <span class="nav-active">Resumen</span>
            <span>Flujos</span>
            <span>Interfaz</span>
            <span>Releases</span>
          </aside>
          <div class="dashboard">
            <div class="dash-heading">
              <div>
                <small>Sprint 08 · release candidate</small>
                <h2>Lista para validar.</h2>
              </div>
              <span class="mock-button">Publicar versión</span>
            </div>
            <div class="metric-row">
              <article>
                <strong data-count="12">12</strong><span>Módulos listos</span>
              </article>
              <article>
                <strong data-count="4">4</strong><span>Flujos validados</span>
              </article>
              <article>
                <strong data-count="98" data-suffix="%">98%</strong><span
                  >Checks OK</span
                >
              </article>
            </div>
            <div class="activity">
              <div class="activity-head">
                <strong>Últimos cambios</strong><span>En curso</span>
              </div>
              <div class="activity-row">
                <i></i><span>Permisos y roles</span><small>Listo</small>
              </div>
              <div class="activity-row">
                <i></i><span>Flujo principal</span><small>Validado</small>
              </div>
              <div class="activity-row">
                <i></i><span>Release candidata</span><small>En QA</small>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .hero-copy {
    animation: hero-in 700ms 100ms both cubic-bezier(0.2, 0.75, 0.25, 1);
  }

  .compiler {
    position: relative;
    min-height: 35rem;
    animation: hero-in 800ms 260ms both cubic-bezier(0.2, 0.75, 0.25, 1);
  }

  .compiler::before {
    position: absolute;
    inset: 8% 3% 2% 12%;
    border-radius: var(--radius-ui);
    background: var(--color-signal);
    content: '';
    transform: rotate(2deg);
  }

  .product-window {
    position: absolute;
    z-index: 1;
    right: 0;
    bottom: 1.5rem;
    width: 91%;
    overflow: hidden;
    border: 1px solid var(--color-ink);
    border-radius: var(--radius-ui);
    background: var(--color-surface);
    box-shadow: var(--shadow-window);
    opacity: 0;
    transform: translateY(1.5rem) scale(0.97);
    animation: product-in 600ms 520ms forwards cubic-bezier(0.23, 1, 0.32, 1);
  }

  .window-bar {
    display: grid;
    grid-template-columns: auto 1fr auto;
    align-items: center;
    gap: 0.75rem;
    height: 3.5rem;
    border-bottom: 1px solid var(--color-line);
    padding: 0 1rem;
    font-size: 0.72rem;
    font-weight: 700;
  }

  .brand-mark {
    display: grid;
    width: 1.75rem;
    aspect-ratio: 1;
    place-items: center;
    border-radius: 0.2rem;
    background: var(--color-ink);
    color: var(--color-surface);
  }

  .brand-mark span,
  .live-state {
    color: var(--color-signal);
  }
  .live-state {
    font-family: var(--font-mono);
  }

  .window-body {
    display: grid;
    grid-template-columns: 7.5rem 1fr;
    min-height: 26rem;
  }

  .window-body aside {
    display: flex;
    flex-direction: column;
    gap: 0.35rem;
    border-right: 1px solid var(--color-line);
    padding: 1.25rem 0.75rem;
    font-size: 0.68rem;
    font-weight: 650;
    color: var(--color-muted);
  }

  .window-body aside span {
    padding: 0.55rem 0.65rem;
  }
  .window-body .nav-active {
    border-radius: 0.2rem;
    background: var(--color-ink);
    color: var(--color-surface);
  }
  .dashboard {
    min-width: 0;
    padding: 1.5rem;
  }
  .dash-heading {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
  }
  .dash-heading small {
    color: var(--color-muted);
    font-size: 0.61rem;
  }
  .dash-heading h2 {
    margin: 0.2rem 0 0;
    font-family: var(--font-display);
    font-size: 1.3rem;
  }
  .dash-heading .mock-button {
    border-radius: 0.2rem;
    background: var(--color-signal);
    padding: 0.65rem 0.75rem;
    color: var(--color-surface);
    font: 700 0.62rem var(--font-body);
  }
  .metric-row {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 0.65rem;
    margin-top: 1.5rem;
  }
  .metric-row article {
    display: flex;
    min-width: 0;
    flex-direction: column;
    border: 1px solid var(--color-line);
    border-radius: 0.2rem;
    padding: 0.8rem;
  }
  .metric-row strong {
    font-family: var(--font-display);
    font-size: 1.35rem;
    font-variant-numeric: tabular-nums;
  }
  .metric-row span {
    margin-top: 0.25rem;
    overflow: hidden;
    color: var(--color-muted);
    font-size: 0.58rem;
    text-overflow: ellipsis;
  }
  .activity {
    margin-top: 0.75rem;
    border: 1px solid var(--color-line);
    border-radius: 0.2rem;
    padding: 0.85rem;
  }
  .activity-head,
  .activity-row {
    display: grid;
    grid-template-columns: 1fr auto;
    align-items: center;
  }
  .activity-head {
    padding-bottom: 0.65rem;
    font-size: 0.66rem;
  }
  .activity-head span {
    color: var(--color-signal);
  }
  .activity-row {
    grid-template-columns: auto 1fr auto;
    gap: 0.55rem;
    border-top: 1px solid var(--color-line);
    padding: 0.65rem 0;
    font-size: 0.58rem;
  }
  .activity-row i {
    width: 0.38rem;
    aspect-ratio: 1;
    border-radius: 50%;
    background: var(--color-signal);
  }
  .activity-row small {
    color: var(--color-muted);
  }

  @keyframes hero-in {
    from {
      opacity: 0;
      transform: translateY(1.25rem);
    }
  }
  @keyframes product-in {
    to {
      opacity: 1;
      transform: none;
    }
  }

  @media (max-width: 63.99rem) {
    .compiler {
      min-height: 34rem;
    }
    .product-window {
      width: 92%;
    }
  }

  @media (max-width: 37.5rem) {
    .compiler {
      min-height: 20rem;
    }
    .product-window {
      bottom: 1rem;
      width: 122%;
      scale: 0.78;
      transform-origin: right bottom;
    }
    .window-body {
      grid-template-columns: 4.4rem 1fr;
      min-height: 16rem;
    }
    .window-body aside {
      padding: 0.9rem 0.35rem;
      font-size: 0.5rem;
    }
    .window-body aside span {
      padding: 0.45rem 0.35rem;
    }
    .dashboard {
      padding: 0.85rem;
    }
    .dash-heading h2 {
      font-size: 0.9rem;
    }
    .dash-heading .mock-button {
      padding: 0.5rem;
      font-size: 0.48rem;
    }
    .metric-row {
      gap: 0.35rem;
      margin-top: 1rem;
    }
    .metric-row article {
      padding: 0.5rem;
    }
    .metric-row strong {
      font-size: 0.9rem;
    }
    .metric-row span {
      font-size: 0.45rem;
    }
    .activity {
      padding: 0.5rem;
    }
    .activity-row {
      gap: 0.3rem;
      font-size: 0.48rem;
    }
  }
</style>
